# Model bundle

Put the on-device model file here:

- `camo_int8.tflite` — the file `CameraScreen.tsx` references via `require(...)`

Generated by:

```bash
python ../../scripts/export_mobile.py --weights ../../models/best.pt --int8 --formats tflite
cp ../../models/best_int8.tflite ./camo_int8.tflite
```

This file is intentionally **not** committed (see top-level `.gitignore`). Each developer builds it from the latest trained checkpoint.
