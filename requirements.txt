# Core training / inference
ultralytics>=8.3.0      # ships YOLO26 + YOLO11; will pull in torch, torchvision
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
PyYAML>=6.0

# Export pipeline (mobile)
onnx>=1.16.0
onnxruntime>=1.18.0
onnxslim>=0.1.31
tensorflow>=2.15.0      # required for TFLite export
coremltools>=7.2 ; sys_platform == "darwin"  # CoreML export only works on macOS

# Evaluation / utilities
matplotlib>=3.7.0
pandas>=2.0.0
tqdm>=4.66.0
seaborn>=0.13.0
